pub fn invert(s: &mut String){
    let l = s.len();
    for i in 0..l {
        let c1 = s.chars()
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let mut s: String = "Test".to_string();
    }
}
